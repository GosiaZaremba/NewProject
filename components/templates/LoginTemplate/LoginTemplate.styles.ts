import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  topImageView: {
    alignItems: 'flex-end',
    justifyContent: 'flex-start',
  },
  bottomImageView: {
    opacity: 0.5,
  },
});
